[package]
name = "hive_mind"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libc = "0.2.153"
tracing = "0.1.40"

[dev-dependencies]
rand = "0.8"
